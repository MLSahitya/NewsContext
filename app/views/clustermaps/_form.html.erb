<%= form_for(@clustermap) do |f| %>
  <% if @clustermap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clustermap.errors.count, "error") %> prohibited this clustermap from being saved:</h2>

      <ul>
      <% @clustermap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :clusid %><br />
    <%= f.text_field :clusid %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
