In this application we read the rss souce links from the asserts folder,i.e. function call  sourceStore()

After all the source links are stored we collect the feeds from each source along with the comlete article.
Once all the details are collected 
title,
summary,
url,
published Date,
full article from the url

then, the title,summary are combined to form a text, which is processed to obtain the Keywords of the article.
 
